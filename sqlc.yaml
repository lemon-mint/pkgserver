version: 1
packages:
  - path: "db"
    name: "db"
    engine: "postgresql"
    schema: "./schema"
    queries: "./queries"
    emit_json_tags: true
    emit_prepared_queries: true
    emit_db_tags: true
    sql_package: "pgx/v4"
